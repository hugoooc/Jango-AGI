name: jango
runtime_name: jango
slug: jango
path: sdk/introspection/recipe
description: Jango is a capability-driven Chief Engineer that delegates, runs real solver fleets, and iterates against measurable goals.
strict: true
runtime:
  kind: byor
  llm_mode: managed
  resources:
    requests:
      cpu: 1000m
      memory: 2Gi
      storage: 10Gi
    limits:
      cpu: 2000m
      memory: 4Gi
