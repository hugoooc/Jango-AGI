GRADIUM_API_KEY=your_gradium_key
HCOMPANY_API_KEY=your_holo_key
# HAI_API_KEY=your_h_platform_key

# Optional container-fleet overrides. The deterministic visual smoke test does
# not require an H Company key; vision-grounded actions do.
LP_MAX_WORKERS=12
LP_WORKER_IMAGE=legacypilot-openvsp-worker:dev
