"""LegacyPilot — APIize a GUI application the agent has never seen.

Discover once (GUI + Holo) -> compile skills + parameter map -> run analyses,
sweeps and trade studies fast, screen-free, forever.
"""
from .skills import Registry, Skill        # noqa: F401
