{
  "name": "jango",
  "version": "1.0.0",
  "description": "Jango: an autonomous task-native Chief Engineer for constrained aircraft TLAR optimization.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "smoke": "node solver/cli.mjs inspect --geometry assets/reference-aircraft.vsp3"
  },
  "keywords": ["pi-package", "engineering", "mdao", "openvsp", "agents"],
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "pi": {
    "agents": ["agents/*.yaml"],
    "skills": ["skills/**/SKILL.md"],
    "extensions": ["extensions/*.ts"]
  }
}
